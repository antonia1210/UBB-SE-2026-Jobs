<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UBB_SE_2026_Jobs.App.Views.TestsAndInterviews.TiManageSlotsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dtos="using:UBB_SE_2026_Jobs.Library.DTOs.TI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="4">
                <TextBlock Text="Manage Interview Slots" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Click an empty cell to add a slot. Click a slot to edit or delete it."
                           FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            <Button Grid.Column="1" Content="Back" VerticalAlignment="Center" Click="Back_Click"/>
        </Grid>

        <!-- Week Navigation -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
            <Button Content="‹" Width="40" Click="PreviousWeek_Click"/>
            <TextBlock Text="{x:Bind ViewModel.WeekLabel, Mode=OneWay}"
                       FontSize="16" FontWeight="SemiBold"
                       VerticalAlignment="Center" MinWidth="180" TextAlignment="Center"/>
            <Button Content="›" Width="40" Click="NextWeek_Click"/>
            <Button Content="Today" Click="Today_Click"/>
        </StackPanel>

        <!-- Legend -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="20" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Border Width="16" Height="16" CornerRadius="2" Background="#DDD6FE"/>
                <TextBlock Text="Available" FontSize="12" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Border Width="16" Height="16" CornerRadius="2" Background="#A5D6A7"/>
                <TextBlock Text="Booked" FontSize="12" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </StackPanel>

        <!-- Status -->
        <TextBlock Grid.Row="3"
                   Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                   Foreground="#22c55e" FontSize="13" Margin="0,0,0,8"/>

        <!-- Calendar: headers + rows built in code-behind into this grid -->
        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <Grid x:Name="CalendarGrid"/>
        </ScrollViewer>
    </Grid>
</Page>