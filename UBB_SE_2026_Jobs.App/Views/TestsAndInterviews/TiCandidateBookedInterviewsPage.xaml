<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UBB_SE_2026_Jobs.App.Views.TestsAndInterviews.TiCandidateBookedInterviewsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dtos="using:UBB_SE_2026_Jobs.App.Dtos.TI"
    xmlns:converters="using:UBB_SE_2026_Jobs.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Page.Resources>
		<converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

		<DataTemplate x:Key="InterviewCardTemplate" x:DataType="dtos:TiInterviewSessionDto">
			<Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="10" Padding="12" Margin="8">
				<StackPanel Spacing="6">
					<TextBlock FontWeight="SemiBold" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}">
						<Run Text="Interview for Position #"/>
						<Run Text="{x:Bind PositionId}"/>
					</TextBlock>
					<TextBlock Text="{x:Bind DateStart}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
					<Border Background="{ThemeResource AccentFillColorDefaultBrush}"
							CornerRadius="4" Padding="8,3"
							HorizontalAlignment="Left">
						<TextBlock Text="{x:Bind Status}"
								   FontSize="11" FontWeight="SemiBold" Foreground="White"/>
					</Border>
				</StackPanel>
			</Border>
		</DataTemplate>
	</Page.Resources>

	<Grid Padding="24">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<StackPanel Grid.Row="0" Margin="0,0,0,16" Spacing="4">
			<TextBlock Text="My Booked Interviews" FontSize="24" FontWeight="Bold"/>
			<TextBlock Text="Here are all your scheduled interview sessions."
                       FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
		</StackPanel>

		<!-- Refresh Button -->
		<Button Grid.Row="1"
                Content="Refresh"
                Margin="0,0,0,16"
                HorizontalAlignment="Left"/>

		<!-- Loading Ring -->
		<ProgressRing Grid.Row="2"
                      IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                      Width="48" Height="48"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>

		<!-- Empty State -->
		<Border Grid.Row="2"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="24"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
			<TextBlock Text="You have no booked interviews yet."
                       FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextAlignment="Center"/>
		</Border>

		<!-- Interviews List -->
		<ScrollViewer Grid.Row="2">
			<ItemsRepeater ItemsSource="{x:Bind ViewModel.BookedInterviews, Mode=OneWay}"
                           ItemTemplate="{StaticResource InterviewCardTemplate}">
				<ItemsRepeater.Layout>
					<UniformGridLayout MinItemWidth="260" MaximumRowsOrColumns="3"
                                       ItemsStretch="Fill" MinColumnSpacing="0" MinRowSpacing="0"/>
				</ItemsRepeater.Layout>
			</ItemsRepeater>
		</ScrollViewer>
	</Grid>
</Page>